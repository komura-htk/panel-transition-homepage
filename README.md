# Sample Homepage

Static sample homepage with five full-screen views and panel-based transitions.

## Files

- `index.html`: page markup
- `styles.css`: full-screen layout and panel styling
- `script.js`: hover, click, scroll, hints, and automatic transition behavior

Serve the directory with any static HTTP server, then open the local URL.
For example: `python -m http.server 8000`.
